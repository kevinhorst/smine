{
  "bash32-compat": {
    "argv": [
      "bin/verifiers/bash32-compat"
    ],
    "timeout_s": 20,
    "description": "fails when a shell script uses a bash-4+ construct — hooks/routines run under macOS bash 3.2 (launchd)"
  },
  "errorf-prefix": {
    "argv": [
      "bin/verifiers/errorf-prefix"
    ],
    "timeout_s": 20,
    "description": "fails when a fmt.Errorf/errors.New message literal lacks the Component.Method: prefix (RULE-GOLANG-ERR-001)"
  },
  "evalgen-bounds": {
    "argv": [
      "bin/verifiers/evalgen-bounds"
    ],
    "timeout_s": 20,
    "description": "fails when a verifier main.go leaves the acdsl/evalgen.json bounds — line count, or an import outside stdlib/internal/go.mod requires (generation may never add a dependency)"
  },
  "exhaustive-switch": {
    "argv": [
      "bin/verifiers/exhaustive-switch"
    ],
    "timeout_s": 60,
    "description": "fails when a switch over a typed-constant enum misses members (ACTION-IMPL-INTEG-005); go tool exhaustive over the anchored packages — requires the exhaustive tool dependency in the target go.mod (go tool exhaustive), or override in registry.local.json"
  },
  "forbid-call": {
    "argv": [
      "bin/verifiers/forbid-call"
    ],
    "timeout_s": 30,
    "description": "fails when any anchored file calls \u003cpkg\u003e.\u003cfunc\u003e (import-alias aware, syntax-level)"
  },
  "func-signature": {
    "argv": [
      "bin/verifiers/func-signature"
    ],
    "timeout_s": 20,
    "description": "fails when a FuncDecl violates RULE-GOLANG-FUNC-001: ctx first (named ctx), error last, at most 3 return values"
  },
  "gofmt": {
    "argv": [
      "bin/verifiers/gofmt"
    ],
    "timeout_s": 30,
    "description": "fails when an anchored file is not gofmt-formatted (wraps gofmt -l; exit-0-with-findings normalized to the contract)"
  },
  "jsonschema": {
    "argv": [
      "bin/verifiers/jsonschema"
    ],
    "timeout_s": 20,
    "description": "fails when an anchored JSON file does not conform to the draft-07 schema named by schema= (santhosh-tekuri/jsonschema/v6)"
  },
  "literal-owner": {
    "argv": [
      "bin/verifiers/literal-owner"
    ],
    "timeout_s": 20,
    "description": "fails when a file outside the allowed path regexp contains the literal"
  },
  "rules-check": {
    "argv": [
      "bin/verifiers/rules-check"
    ],
    "timeout_s": 60,
    "description": "wraps cmd/rules by mode= param: validate (ACTION/FACT contract) or generate-check (rules.json currency); replaces the raw Makefile audit lines"
  },
  "shellcheck": {
    "argv": [
      "bin/verifiers/shellcheck"
    ],
    "timeout_s": 30,
    "description": "fails on shellcheck findings over the anchored scripts (brew dep, accepted Band D); missing binary is a tool error, never a silent pass"
  },
  "skill-context": {
    "argv": [
      "bin/verifiers/skill-context"
    ],
    "timeout_s": 30,
    "description": "validates the SKILL.md frontmatter context: declaration — token grammar (entry ID or trailing-* glob), exact IDs exist in the index= file, globs match at least one entry"
  },
  "skill-entries": {
    "argv": [
      "bin/verifiers/skill-entries"
    ],
    "timeout_s": 30,
    "description": "validates the SKILL.md entry grammar — **SKILL-\u003cNAME\u003e-\u003cTOPIC\u003e-NNN** `[class]` — statement headlines, NAME = leaf dir upper-cased without hyphens, known class tags, unique ids, payload entries followed by one fenced block; skills without entries pass"
  },
  "skill-meta": {
    "argv": [
      "bin/verifiers/skill-meta"
    ],
    "timeout_s": 20,
    "description": "SKILL.md metadata checks by mode= param: version-sync (frontmatter version == changelog.json[0].version), frontmatter (required single-line keys + description shape), allowed-tools (permission-rule dialect)"
  },
  "skill-plan-template": {
    "argv": [
      "bin/verifiers/skill-plan-template"
    ],
    "timeout_s": 30,
    "description": "validates a SKILL.md plan-template: declaration — the named [payload] entry exists, its template opens with ## TLDR, catalog sections (rules/plan.md RULE-PLAN-002) keep catalog order, non-catalog sections carry an \u003coverride: placeholder; skills without the key pass"
  },
  "test-schema": {
    "argv": [
      "bin/verifiers/test-schema"
    ],
    "timeout_s": 20,
    "description": "fails when t.Run names a case with anything but the house schema's test._id (RULE-GOLANG-TEST-001..005)"
  }
}
