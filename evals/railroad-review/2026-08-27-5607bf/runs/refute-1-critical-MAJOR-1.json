{
  "finding_id": "critical-MAJOR-1",
  "verdict": "debunked",
  "reasoning": "The claim's load-bearing assertion is that a stale $repo_root/.git/worktrees/<id> registration (dir purged from $RR_CELLS, entry surviving) bricks that cell id 'on every subsequent run until a human runs git worktree prune by hand'. That is false: run.sh already runs exactly that command automatically on every run, before any cell is created. run.sh:87 sources routines/_lib/worktree.sh; run.sh:94 calls routine_worktree_create; that function executes a bare `git -C \"$repo_root\" worktree prune` at worktree.sh:133 (unconditionally, immediately after removing the pool worktree, well before the lane loop at run.sh:194 that reaches run_cell line 154). A bare `git worktree prune` with no --expire defaults to expire=TIME_MAX in git's prune(), so every registration whose working-tree directory is missing is pruned immediately — not subject to the 3-month gc.worktreePruneExpire grace, which only git gc applies. The cell worktrees register under $repo_root/.git/worktrees/ (added via `git -C \"$repo_root\" worktree add`, run.sh:154), the same admin dir prune scans, so the global prune clears the stale <id> entry regardless of its $RR_CELLS path. Consequently, on the next run the stale registration is gone before run_cell line 154's `git worktree add`, and the add succeeds. The routine self-heals every run; there is no permanent brick and no human intervention needed. The lane's mistake is explicit in its own evidence: it scoped its grep to run.sh alone ('grep across run.sh confirms no worktree prune') and never followed the two sourced libs (worktree.sh, line 87) that run.sh unconditionally invokes.",
  "evidence": "run.sh:87 `source \"$repo_root/routines/_lib/worktree.sh\"`; run.sh:94 `wt=\"$(routine_worktree_create)\"` (before the Stage-1 lane loop at run.sh:194 and thus before any run_cell/line-154 worktree add). worktree.sh:126-135 routine_worktree_create: line 131 removes the group worktree, then line 133 `git -C \"$repo_root\" worktree prune` (bare, no --expire). Git's builtin worktree prune() initializes `expire = TIME_MAX` before option parsing, so absent --expire the mtime guard `st.st_mtime <= expire` is always true and any worktree with a missing working-tree directory is pruned unconditionally (the 3-month gc.worktreePruneExpire default is passed only by git gc, not by a direct `git worktree prune`). Cell worktrees live in the same $repo_root/.git/worktrees/ that prune scans (run.sh:154 `git -C \"$repo_root\" worktree add ... \"$cellwt\"`, cellwt=$RR_CELLS/$id). The claim itself names `git worktree prune` as the human remedy (finding claim + fix), which run.sh performs automatically each run. The lane's grep was scoped to run.sh only ('grep across run.sh confirms no worktree prune'), missing the sourced worktree.sh."
}
