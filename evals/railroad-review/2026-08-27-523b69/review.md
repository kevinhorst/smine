# Station review — 2026-08-27 — 72f50e5..523b69

Scope: the four newly bundled workflows and their fronting skills. Correctness lane basis: docs/workflows.md (the request contract) plus each skill's changelog.json/SKILL.md. No refuter stage ran for this round (no refute-*.json artifacts), so every surviving claim carries `refuter_verdict: null` and was re-verified directly against this checkout.

| ID | Severity | Route | Location | Finding | Proposed fix |
|----|----------|-------|----------|---------|--------------|
| correctness-MIN-1 | MINOR | human | skills/feature/fdesign/SKILL.md:60 | subsystem-grounding.js is documented (docs/workflows.md:94) as fronted by /fdesign, but fdesign SKILL.md never references or invokes it — the bundled workflow is inert, so multi-subsystem grounding never runs. | Add a Phase-1 bullet invoking Workflow({scriptPath:'<base>/workflows/subsystem-grounding.js', args:{subsystems,drivers,planRef}}), mirroring smine SKILL.md:13. |
| correctness-MIN-2 | MINOR | human | skills/feature/fimplement/SKILL.md:62 | config-ui-fidelity-gate.js is documented (docs/workflows.md:86) as a pre-first-commit gate fronted by /fimplement, but fimplement SKILL.md never invokes it — the gate never fires, so an off-spec config UI can be committed unchecked. | Add a pre-commit bullet invoking Workflow({scriptPath:'<base>/workflows/config-ui-fidelity-gate.js', args:{templates,cssPath,renderHint}}) when the diff touches config-server templates or style.css. |
| correctness-MIN-3 | MINOR | human | skills/git/package-commit/SKILL.md:20 | foreign-toolchain-pretag.js is documented (docs/workflows.md:102) as fronted by /package-commit, but the SKILL.md diff only bumps the version — no invocation — so the local pre-tag compile never runs and a pushed tag's CI stays the first compile. | Add a release-tag step invoking Workflow({scriptPath:'<base>/workflows/foreign-toolchain-pretag.js', args:{artifacts,tagRef}}), refusing to tag on gate.pass=false. |
| code-style-NIT-1 | NIT | auto-fix | skills/feature/fimplement/workflows/config-ui-fidelity-gate.js:73 | phase('Gate') sits inside the pipeline second-stage callback after an early return, so it never fires for render-failed templates and re-emits per item — the only file nesting a phase() marker in a loop body; every sibling (incl. foreign-toolchain-pretag.js:58, same Build/Gate shape) hoists phase() to top level. | Move phase('Gate') to top level after the pipeline() call, or split Render/Gate into two sequential passes. |
| code-style-NIT-2 | NIT | auto-fix | skills/git/package-commit/workflows/foreign-toolchain-pretag.js:66 | JS-side const/return key `blocking_errors` is snake_case (repo reserves snake_case for agent-output schema fields); the sibling gate config-ui-fidelity-gate.js uses `violations`, so the two gate workflows return divergent keys for the same shape. | Rename to camelCase and align with the sibling: gate: { pass, violations } (or blockingErrors). |
| correctness-INF-1 | INFO | human | skills/smine/smine/SKILL.md:46 | session-mine.js v1.13 added optional model/effort tier-override args (docs/workflows.md:45), but the smine SKILL.md resolve-step arg list omits them — doc-completeness gap only, no runtime defect. | Note model/effort as optional tier overrides in the resolve step or the manual-run reference. |

Dedup: the two code-style lanes raised the same phase('Gate') defect; merged to code-style-NIT-1 (merged_from: code-style-1/code-style-NIT-1, code-style-2/code-style-NIT-1), max group severity NIT. Correctness lane 1 and both critical lanes returned clean.

Funnel: claims produced 7 / after dedup 6 / debunked 0 / survivors 6.
